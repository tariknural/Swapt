/**
 * Class for commonly used functions among services/controllers.
 */

/**
 * 
 * @param {*} req object as input, returns a map for given filter parameters. field is empty if its not provided.  
 */
function mapFilterParametersFromRequest(req){
    return {};
}

module.exports = {mapFilterParametersFromRequest};